// Arduino IDE entry sketch for PAPER-GO.
// setup() and loop() are implemented in src/main.cpp.
//
// These includes intentionally live in the .ino so Arduino IDE's
// library resolver pulls the correct library include paths before
// compiling files under src/.

#include <Arduino.h>
#include "epd_driver.h"
